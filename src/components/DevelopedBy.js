import React from 'react';
import { withStyles } from '@material-ui/core/styles';

const styles = theme => ({});

const DevelopedBy = ({}) => {
    return (<div></div>)
}

export default withStyles(styles)(DevelopedBy)